# the enumaret function while looping
# enumerate is used when one wants to retreive both the position index and the corresponding value

for i, v in enumerate(['tic', 'tac', 'toe']):
    print(i, v)
